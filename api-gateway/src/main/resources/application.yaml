server:
  port: 8181

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: test-route
          uri: http://httpbin.org
          predicates:
            - Path=/test

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka